pipeline {
    agent any

    stages {
        stage ('Build Stage') {
            steps {
                echo 'Build Stage'
            } 
        }

        stage ('Testing Stage') {
            steps {
                echo 'Testing Stage'
            } 
        }

    }
}
